# CppPilot 四个对话工作清单

这份清单用于和队友快速同步，不按论文格式写，只讲实际做完了什么。

## 对话 1：工程底座

- 搭好 Electron + Vue + TypeScript + pnpm workspace 项目骨架。
- 做了主进程、Preload、Renderer 的安全边界，IPC 都走共享契约和 Zod 校验。
- 做了工作区、项目、文件树、搜索、快照、恢复和 SQLite WAL 数据库。
- 留下了 H1 架构、运行手册、接收清单和测试报告。

## 对话 2：C++ 开发能力

- 接入 GCC、Clang、MSVC、CMake、CTest、clang-tidy、clangd、GDB/MI 和 VS Code 联动。
- 编辑器支持 Monaco 多标签、补全、悬停、定义跳转、编译诊断、运行输出、断点调试。
- 加了受限子进程、超时、取消、输出上限和 Windows 进程树回收。
- 做了中文路径、CMake 工程、样例库和 H2 交接资料。

## 对话 3：Agent 和学习闭环

- 做了 TypeScript Agent Runtime：意图、上下文、计划、审批、工具调用、验证和 Timeline。
- 接了 MCP Host/Worker、本地工具目录、OpenAI-compatible BYOK 网关和离线规划器。
- 做了知识树、知识边界、错题本、复习计划、XP、等级、成长阶段和成就。
- 工作区 Agent、助教记录、审批卡、模型设置和学习页面形成了 H3 基线。

## 对话 4：最终收尾 1-4

- 完成透明桌宠、托盘、快捷键、隐藏一小时、专注模式、开机启动和快速聊天。
- 修了自定义桌宠素材持久化：切回 Logo 不删素材列表，PNG/JPG/WEBP/GIF 上传后能立即切换。
- 桌宠原始窗口改小到 `180 x 220`，拖拽仍只改位置，不因为碰边或 scale 变化缩小窗口。
- 成长展示改成桌宠下方赛博风通关进度条，文案改短，避免小窗口里塞不下。
- 修了工作区右侧 Agent 同步问题，模型审批、请求失败、完成、取消等状态都会同步到同一条对话消息。
- 扩展 XP 闭环：代码有效保存、OJ 提交/通过、知识点通关、项目任务和成就奖励都能进经验。
- 练习页改成 OJ 体验：题单、题面、代码编辑、提交、5 用例判题、100 分评分和失败输出差异。
- OJ 截图导入继续走 `input_image`，模型必须给出 5 个可信用例，不足就拒绝添加。
- 新建 README skill，归档旧 README，重写最终 README，补了项目总结材料。
- 最终交付只保留 NSIS 一键安装包口径，完整验证、合并 main、推送远端在收尾最后执行。